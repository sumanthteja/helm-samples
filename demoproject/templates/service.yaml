apiVersion: v1
kind: Service
metadata:
  name: {{ include "demoproject.fullname" . }}
  labels:
    {{- include "demoproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demoproject.selectorLabels" . | nindent 4 }}
