apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demoproject.fullname" . }}-test-connection"
  labels:
    {{- include "demoproject.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demoproject.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
